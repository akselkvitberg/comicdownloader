{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Comic Downloader",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}",
            "cwd": "${workspaceFolder}",
            "env": {
                "PORT": "8080"
            }
        }
    ]
}
